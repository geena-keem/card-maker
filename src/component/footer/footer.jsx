import React from 'react';
import styles from './footer.module.css';

const Footer = (props) => (
    <footer className={styles.footer}>
        <p className={styles.title}>지나지나지나지나지나</p>
    </footer>
);

export default Footer;
